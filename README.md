# drone audit

```shell
$ ./drone-audit
NAME:
   drone-audit - A new cli application

USAGE:
   drone-audit [global options] command [command options] [arguments...]

COMMANDS:
   images
   pipelines
   platforms
   secrets
   volumes
   help, h    Shows a list of commands or help for one command

GLOBAL OPTIONS:
   --debug, -d  Turn on verbose debug logging (default: false)
   --quiet, -q  Turn on off all logging (default: false)
   --help, -h   show help (default: false)
```